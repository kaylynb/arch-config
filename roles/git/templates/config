[core]
	autocrlf = false
[user]
	email = {{ email }}
	name = {{ full_name }}
	signingkey = {{ gpg_signing_key_hash }}
[push]
	default = simple
	followTags = true
[commit]
	gpgsign = true
{% if ansible_facts['os_family']|lower == 'windows' %}
[gpg]
	program = %USERPROFILE%/scoop/apps/gpg/current/bin/gpg.exe
{% endif %}
